{
    "censor_model": {
        "description": "审查模型",
        "type": "string",
        "hint": "nudenet 是轻量本地模型; sightengine 需官网注册并配置 API",
		"options": ["nudenet", "sightengine"],
        "default": "nudenet"
    },
    "sightengine_config": {
        "description": "Sightengine API 配置",
        "type": "object",
        "items": {
            "api_user": {
                "description": "用户名",
                "type": "string",
                "default": ""
            },
            "api_secret": {
                "description": "密钥",
                "type": "string",
                "default": ""
            }
        }
    },
    "blur_scale": {
        "description": "模糊比例",
        "type": "int",
        "hint": "对需审查的图片做全图高斯模糊, scale 越小模糊程度越高，默认 20",
        "default": 20
    }
}